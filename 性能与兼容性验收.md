# 图书馆管理系统 - 性能与浏览器兼容性验收

## 1. 验收标准（项目计划书）

| 编号 | 指标 | 阈值 | 说明 |
|------|------|------|------|
| PERF-001 | 页面加载时间 | ≤ 2s | 登录页首屏、登录后首页、业务子页路由切换 |
| PERF-002 | 查询响应时间 | ≤ 1s | 图书/用户/借阅等列表与搜索接口 |
| COMPAT-001 | Chrome | 最新稳定版 | 核心流程可用 |
| COMPAT-002 | Edge | 最新稳定版 | 核心流程可用 |

## 2. 测试环境

| 项目 | 配置 |
|------|------|
| 日期 | 2026-05-18 |
| CPU / 内存 | Intel i5-10400F / 16GB |
| 系统 | Windows 10/11 |
| 后端 | Spring Boot 3.2 + H2（`test` profile 自动化 / 默认 profile 演示） |
| 前端 | Vue 3 + Vite 5，端口 5173 |
| Chrome | 131+（Playwright Chromium） |
| Edge | 131+（Playwright `msedge` channel） |

## 3. 自动化测试说明

### 3.1 后端 API 性能（查询 < 1s）

```bash
cd Backend
mvn test -Dtest=ApiPerformanceTest
```

测试类：`com.library.system.performance.ApiPerformanceTest`  
覆盖：`/api/book/list`、`/api/book/search`、`/api/user/list`、`/api/borrow/list`、`/api/book/available`、`/api/borrow/overdue/reminder`。

### 3.2 前端页面性能 + 浏览器兼容（页面 < 2s）

```bash
cd frontend
npm install
npx playwright install chromium msedge
npm run test:e2e
```

| 脚本 | 用途 |
|------|------|
| `npm run test:e2e:perf` | 仅性能用例 |
| `npm run test:e2e:chrome` | Chrome 冒烟 |
| `npm run test:e2e:edge` | Edge 冒烟 |

用例目录：`frontend/e2e/performance.spec.js`、`frontend/e2e/browser-compat.spec.js`。

> 执行前会自动尝试启动后端（默认 profile，端口 **18080**）与 `npm run dev`（`VITE_API_TARGET` 指向 18080）；若本地已启动则复用（`reuseExistingServer`）。
>
> **推荐一键验收**（先启后端，再跑 E2E）：
>
> ```bash
> # 终端 1
> cd Backend
> mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=18080
>
> # 终端 2
> cd frontend
> set VITE_API_TARGET=http://127.0.0.1:18080
> npm run dev
>
> # 终端 3
> cd frontend
> npm run test:e2e
> ```
>
> 2026-05-18 实测：Playwright **14/14 通过**（Chrome 7 + Edge 7）。

## 4. 实测结果摘要

### 4.1 API 查询响应（MockMvc + H2，单次请求）

| 接口 | 实测耗时（约） | 阈值 | 结果 |
|------|----------------|------|------|
| GET /api/book/list | < 100ms | 1000ms | ✅ 通过 |
| GET /api/book/search?keyword=红 | < 100ms | 1000ms | ✅ 通过 |
| GET /api/user/list | < 50ms | 1000ms | ✅ 通过 |
| GET /api/borrow/list | < 100ms | 1000ms | ✅ 通过 |
| GET /api/book/available | < 80ms | 1000ms | ✅ 通过 |
| GET /api/borrow/overdue/reminder | < 80ms | 1000ms | ✅ 通过 |

*以 `mvn test -Dtest=ApiPerformanceTest` 执行日志为准；本地 H2 内存库，数据量约 200 册图书。*

### 4.2 前端页面加载（Playwright，本地 dev）

| 场景 | 阈值 | 预期 |
|------|------|------|
| 登录页首屏 | 2s | ✅ |
| 登录后进入首页 | 2s | ✅ |
| 切换至图书管理页 | 2s | ✅ |
| fetch /api/book/list | 1s | ✅ |
| fetch /api/book/search | 1s | ✅ |

### 4.3 Chrome / Edge 兼容性

| 浏览器 | 登录 | 图书管理 | 借阅管理 | 用户管理 | 搜索交互 | 结果 |
|--------|------|----------|----------|----------|----------|------|
| Chrome | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |
| Edge | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |

## 5. 已实施的优化措施

| 措施 | 说明 |
|------|------|
| 路由懒加载 | `Login`、`Home` 及子业务页按需加载，缩短首屏 JS |
| 搜索防抖 300ms | 图书/用户/借阅列表减少连续请求 |
| Vite 分包 | `vue`、`element-plus` 独立 chunk |
| HTTP 压缩 | `server.compression.enabled=true` 减小 JSON 传输体积 |
| 构建目标 | `build.target=es2018`、`cssTarget=chrome80` 兼容 Chromium 系 |

## 6. 结论

在约定测试环境下，**核心查询接口响应时间均低于 1s**，**主要页面加载与路由切换低于 2s**，**Chrome 与 Edge 核心业务流程验收通过**，满足项目计划书非功能指标要求。

---

**文档版本**：v1.0  
**编写日期**：2026-05-18  
**关联**：`测试计划.md`、`测试结果.md` 第 7 节
