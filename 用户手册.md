# 图书馆管理系统 - 用户手册

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 用户手册 |
| 文档版本 | v1.0 |
| 适用对象 | 管理员、读者、部署与验收人员 |
| 创建日期 | 2026-05-18 |

---

## 2. 系统简介

图书馆管理系统用于管理馆藏图书、系统用户及借还记录。系统分为 **管理员** 与 **读者** 两种角色：管理员负责用户与全馆业务；读者可检索图书、办理本人借还与查看借阅记录。

**数据说明**：系统使用 **H2 内存数据库**，每次**重启后端**会按内置脚本重新导入演示数据，此前在界面中的增删改（除当前未重启的会话外）不会永久保留。

---

## 3. 运行环境与启动

### 3.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（或其他支持 JDK 的系统） |
| JDK | 17+ |
| Node.js | 18+（仅前端开发/构建需要） |
| 浏览器 | Chrome 或 Edge 最新稳定版 |
| 数据库 | **无需安装 MySQL**；后端内置 H2 |

### 3.2 启动步骤

**1）启动后端**

```bash
cd Backend
mvn spring-boot:run
```

启动成功后 API 地址：`http://localhost:8080`

**2）启动前端（开发模式）**

```bash
cd frontend
npm install
npm run dev
```

浏览器访问：`http://localhost:5173`

> 前端通过 Vite 将 `/api` 请求代理到 `http://localhost:8080`，请确保后端已先启动。

### 3.3 可选：H2 数据库控制台

仅用于开发调试，地址：`http://localhost:8080/h2-console`

| 项 | 值 |
|----|-----|
| JDBC URL | `jdbc:h2:mem:library_db` |
| 用户名 | `sa` |
| 密码 | （留空） |

---

## 4. 登录与退出

### 4.1 演示账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | password | 可进入用户管理、全馆借阅 |
| 读者 | user1 | user123 | 首页可看到逾期演示提醒 |
| 读者 | user2 | user123 | 普通读者 |

### 4.2 登录操作

1. 打开系统登录页。
2. 输入用户名、密码。
3. 点击「登录」或按 Enter。
4. 登录成功进入首页；失败时页面提示「用户名或密码错误」。

### 4.3 退出操作

1. 点击右上角 **退出登录**。
2. 系统调用服务端注销接口并清除本地登录信息，返回登录页。

---

## 5. 界面总览

登录后左侧为导航菜单，右侧为内容区。不同角色菜单文案略有区别：

| 菜单（管理员） | 菜单（读者） | 功能 |
|----------------|--------------|------|
| 首页 | 首页 | 欢迎页、统计、逾期提醒 |
| 个人中心 | 个人中心 | 查看当前账号信息 |
| 用户管理 | （无） | 仅管理员 |
| 图书管理 | 图书检索 | 图书列表与搜索 |
| 借阅管理 | 我的借阅 | 借还记录与操作 |

---

## 6. 管理员操作指南

### 6.1 首页

- 查看用户总数、馆藏册数、可借册数等统计。
- 若存在逾期未还记录，顶部显示红色 **逾期提醒**，列出书名、应还日、逾期天数及借阅人。
- 可点击链接跳转到 **借阅管理** 处理归还。

### 6.2 用户管理

**查询**

- 进入「用户管理」，列表展示全部用户。
- 在搜索框输入关键词可筛选用户名等。

**新增用户**

1. 点击「添加用户」。
2. 填写用户名、密码、角色（管理员 / 读者）、邮箱（可选）。
3. 确认保存。
4. 注意：系统**只允许一名管理员**；若已有管理员，再添加管理员会提示失败。

**编辑用户**

1. 点击某行的「编辑」。
2. 修改信息；若**不修改密码**，请将密码框**留空**（系统保留原密码）。
3. 保存。

**删除用户**

1. 点击「删除」，在确认框中选择确定。
2. 删除后用户编号可能重新连续编号（系统内部维护）。

### 6.3 图书管理

**查询与搜索**

- 列表展示全部图书（含总册数、可借册数）。
- 搜索框支持按书名、作者、出版社、ISBN 等关键词模糊查询。

**新增图书**

1. 点击「添加图书」。
2. 填写书名、作者、ISBN、出版社、册数等。
3. 保存后表单会重置，可继续录入下一本。

**编辑 / 删除**

- 编辑：修改信息后保存；修改总册数时系统会同步可借数量规则。
- 删除：确认后删除该书记录。

**重复图书处理**

- 通过系统提供的重复检测能力（管理端相关入口或接口）查看并清理重复书目（按实现界面为准）。

### 6.4 借阅管理

**查看记录**

- 展示全馆借阅记录：借阅人、书名、借出日、应还日、状态等。
- 支持按书名、用户名关键词筛选。

**办理借书**

1. 点击「借书」或同类按钮。
2. 选择借阅用户、图书、应还日期。
3. 确认后扣减可借册数并生成状态为「借阅中」的记录。
4. 若可借册数为 0，提示不可借。

**办理还书**

1. 在列表中找到「借阅中」或「逾期」记录。
2. 点击「归还」并确认。
3. 可借册数加 1，记录变为「已归还」。

**删除借阅记录**

- 仅用于纠错或演示数据维护；删除后记录 ID 可能重排。

---

## 7. 读者操作指南

### 7.1 首页

- 查看馆藏与可借统计。
- 若本人有逾期图书，首页显示逾期提醒（不含他人信息）。
- 点击可进入「我的借阅」办理归还。

### 7.2 图书检索

1. 进入「图书检索」。
2. 浏览列表或使用搜索框查找图书。
3. 查看「可借册数」判断是否可借。
4. 读者界面一般**不能**新增或删除图书（无管理按钮）。

### 7.3 我的借阅

**查看**

- 仅显示当前登录用户的借阅记录及状态：借阅中、逾期、已归还。

**借书**

1. 点击借书相关按钮。
2. 选择图书与应还日期（用户默认为本人）。
3. 提交后等待成功提示。

**还书**

1. 在「借阅中」或「逾期」记录上点击归还。
2. 确认后完成还书。

**搜索**

- 可按书名等关键词过滤自己的记录。

### 7.4 个人中心

- 查看当前登录用户名、角色等基本信息。
- 密码修改若界面未开放，请联系管理员在「用户管理」中重置。

---

## 8. 借阅状态说明

| 界面显示（示例） | 含义 | 建议操作 |
|------------------|------|----------|
| 借阅中 | 未还且未逾期 | 到期前归还 |
| 逾期 | 超过应还日未还 | 尽快归还 |
| 已归还 | 已完成还书 | 仅查询历史 |

---

## 9. 常见问题（FAQ）

**Q1：重启后端后，我添加的数据不见了？**  
A：正常。H2 使用内存库，重启会重新执行 `data.sql` 导入演示数据。课程演示请在一次启动会话内操作，或自行改造为文件型 H2 / MySQL 持久化。

**Q2：提示「请先登录」？**  
A：登录已过期或未带 Token。请重新登录；勿在多标签页混用不同账号缓存。

**Q3：读者无法打开「用户管理」？**  
A：该菜单仅管理员可见，属权限设计。

**Q4：admin 登录失败？**  
A：确认密码为 `password`（全小写）；确认后端已启动且端口 8080 未被占用。

**Q5：前端页面空白或接口报错？**  
A：检查后端是否运行；检查 `frontend` 开发服务是否启动；浏览器控制台查看是否为网络或 CORS 问题。

**Q6：系统是否使用 MySQL？**  
A：**否**。当前版本默认使用 H2 内存库，无需安装 MySQL。

---

## 10. 浏览器建议

- 推荐使用 **Chrome** 或 **Edge** 最新版。
- 请启用 JavaScript；建议窗口宽度 ≥ 1280px 以获得最佳表格布局。

---

## 11. 联系与文档索引

| 文档 | 用途 |
|------|------|
| `需求分析.md` | 功能与非功能需求 |
| `系统设计.md` | 架构、数据库、接口 |
| `测试计划.md` / `测试用例.md` | 测试说明 |
| `性能与兼容性验收.md` | 性能与浏览器指标 |
| `bug记录.md` / `修复验证.md` | 缺陷与修复记录 |

---

## 12. 修订记录

| 版本 | 日期 | 修订说明 |
|------|------|----------|
| v1.0 | 2026-05-18 | 初稿，基于 H2 内存库与当前前端页面编写 |
