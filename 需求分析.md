# 图书馆管理系统 - 需求分析

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 需求分析 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-05-18 |
| 项目名称 | 校园图书借阅管理系统 |
| 代码仓库 | https://github.com/zzw123ai/library-system |

---

## 2. 项目背景与目标

### 2.1 背景

高校与小型机构需要一套轻量、可演示的图书借阅管理平台，用于完成课程设计或实训考核：覆盖用户与权限、馆藏维护、借还流程及逾期提醒，并能在本地一键启动、无需单独安装 MySQL 等外部数据库服务。

### 2.2 建设目标

| 目标编号 | 描述 |
|----------|------|
| OBJ-001 | 实现管理员与读者两类角色的分权使用 |
| OBJ-002 | 提供图书、用户、借阅三大业务模块的完整 CRUD 与检索 |
| OBJ-003 | 支持借书、还书、逾期状态自动更新与首页提醒 |
| OBJ-004 | 采用前后端分离架构，接口统一 JSON 返回 |
| OBJ-005 | 使用 **H2 内存数据库**，启动时自动建表并导入演示数据 |
| OBJ-006 | 满足非功能指标：主要页面加载 ≤2s、查询接口 ≤1s，兼容 Chrome / Edge |

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 数据标识（`user.role`） | 说明 |
|------|-------------------------|------|
| 管理员 | `1` | 全馆用户、图书、借阅管理；仅允许系统中存在一名管理员 |
| 读者 | `0`（或其他非 `1`） | 图书检索、本人借阅与归还、个人中心 |

### 3.2 权限矩阵

| 功能 | 管理员 | 读者 |
|------|:------:|:----:|
| 登录 / 退出 | ✓ | ✓ |
| 首页统计与逾期提醒（全馆） | ✓ | 仅本人逾期 |
| 用户管理（增删改查） | ✓ | ✗ |
| 图书管理（增删改查、重复书清理） | ✓ | 仅检索与查看可借数量 |
| 借阅管理（全馆记录） | ✓ | 仅「我的借阅」 |
| 代他人借书 / 还书 | ✓ | 仅本人 |
| 个人中心（查看信息） | ✓ | ✓ |
| H2 控制台（开发调试） | 开发环境可访问 | 同左 |

前端通过路由 `meta.adminOnly` 与 `isAdmin()` 拦截无权限页面；后端除登录、H2 控制台外，`/api/**` 均需携带有效 Token。

---

## 4. 功能需求

### 4.1 用户管理模块（FR-USER）

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-USER-001 | 用户登录：校验用户名与密码，成功返回用户信息与 Token | P0 |
| FR-USER-002 | 用户退出：服务端注销 Token，前端清除本地会话 | P0 |
| FR-USER-003 | 获取当前登录用户（`/api/user/me`） | P1 |
| FR-USER-004 | 管理员查询用户列表、按关键词搜索 | P0 |
| FR-USER-005 | 管理员新增用户；角色为管理员时系统仅允许一名 | P0 |
| FR-USER-006 | 管理员编辑用户；密码为空时不修改原密码（BCrypt） | P0 |
| FR-USER-007 | 管理员删除用户；删除后用户 ID 可重排以保持连续 | P1 |

### 4.2 图书管理模块（FR-BOOK）

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-BOOK-001 | 图书列表、按 ID 查询、可借图书列表 | P0 |
| FR-BOOK-002 | 关键词搜索：支持书名、作者、出版社、ISBN 等字段模糊匹配 | P0 |
| FR-BOOK-003 | 管理员新增 / 编辑 / 删除图书；维护 `quantity` 与 `available` 一致性 | P0 |
| FR-BOOK-004 | 重复图书检测与批量清理（管理员工具接口） | P1 |
| FR-BOOK-005 | 删除图书记录后 ID 重排（与借阅、用户模块策略一致） | P1 |

### 4.3 借阅管理模块（FR-BORROW）

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-BORROW-001 | 借书：扣减可借册数（`decreaseAvailable`），写入借阅记录，状态为 `BORROWED` | P0 |
| FR-BORROW-002 | 还书：恢复可借册数，记录归还时间，状态为 `RETURNED` | P0 |
| FR-BORROW-003 | 管理员查看全馆借阅列表；读者仅查看本人记录 | P0 |
| FR-BORROW-004 | 借阅列表支持按书名、用户名关键词筛选 | P1 |
| FR-BORROW-005 | 应还日早于当日且未归还的记录自动标记为 `OVERDUE` | P0 |
| FR-BORROW-006 | 首页逾期提醒：展示逾期书目、应还日期、逾期天数 | P0 |
| FR-BORROW-007 | 管理员删除借阅记录并重排 ID | P1 |

### 4.4 系统与数据模块（FR-SYS）

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-SYS-001 | 应用启动时执行 `schema.sql`、`data.sql` 初始化 H2 库 | P0 |
| FR-SYS-002 | 预置演示账号与约 200 册图书样本数据 | P1 |
| FR-SYS-003 | 预置一条逾期演示借阅（读者 `user1` 借《红楼梦》）便于验收 | P2 |
| FR-SYS-004 | 数据迁移接口（补字段、密码长度、去重、ID 重排等，供升级场景） | P2 |
| FR-SYS-005 | 统一响应体 `Result`：`code`、`message`、`data` | P0 |
| FR-SYS-006 | 鉴权开关 `app.auth.enabled`（默认开启） | P2 |

---

## 5. 非功能需求

### 5.1 性能

| 编号 | 指标 | 要求 |
|------|------|------|
| NFR-PERF-001 | 页面加载 | 登录页、首页及业务子页路由切换 ≤ **2 秒** |
| NFR-PERF-002 | 查询响应 | 图书 / 用户 / 借阅等列表与搜索接口 ≤ **1 秒** |
| NFR-PERF-003 | 传输 | 后端开启 Gzip 压缩（JSON 等） |

验收依据见 `性能与兼容性验收.md`；自动化：`ApiPerformanceTest`、Playwright E2E。

### 5.2 兼容性

| 编号 | 要求 |
|------|------|
| NFR-COMPAT-001 | Google Chrome 最新稳定版核心流程可用 |
| NFR-COMPAT-002 | Microsoft Edge 最新稳定版核心流程可用 |

### 5.3 安全与可用性

| 编号 | 要求 |
|------|------|
| NFR-SEC-001 | 密码存储支持 BCrypt；登录校验兼容历史明文（迁移后统一加密） |
| NFR-SEC-002 | 未登录访问业务 API 返回统一未授权 JSON |
| NFR-SEC-003 | 前端使用 Element Plus 消息与确认框，避免原生 `alert` / `confirm` |
| NFR-UX-001 | 侧栏菜单按角色显示不同文案（如「图书检索」「我的借阅」） |
| NFR-UX-002 | 全局过渡动画与登录页样式优化 |

### 5.4 数据与部署约束

| 编号 | 要求 |
|------|------|
| NFR-DB-001 | **运行期使用 H2 内存库**（`jdbc:h2:mem:library_db`），`MODE=MySQL` 以兼容 SQL 语法 |
| NFR-DB-002 | **不使用 MySQL 作为当前部署数据库**；`pom.xml` 中 MySQL 驱动仅为可选依赖，默认未启用 |
| NFR-DB-003 | 进程重启后数据按 `data.sql` 重新初始化（内存库不持久化） |
| NFR-DB-004 | 开发环境可访问 H2 Console：`http://localhost:8080/h2-console` |

---

## 6. 数据需求（概念）

### 6.1 核心实体

| 实体 | 主要属性 | 说明 |
|------|----------|------|
| 用户（user） | 用户名、密码、邮箱、角色、时间戳 | 用户名唯一 |
| 图书（book） | 书名、作者、ISBN、出版社、总册数、可借册数 | ISBN 唯一（可为空） |
| 借阅记录（borrow_record） | 用户 ID、图书 ID、借出日、应还日、归还日、状态 | 外键关联用户与图书 |

### 6.2 借阅状态

| 状态值 | 含义 |
|--------|------|
| `BORROWED` | 借阅中 |
| `OVERDUE` | 已逾期（未归还且超过应还日） |
| `RETURNED` | 已归还 |

### 6.3 初始演示数据

| 类型 | 内容 |
|------|------|
| 用户 | `admin` / `password`（管理员）；`user1`、`user2` / `user123`（读者） |
| 图书 | 约 200 本经典中文书目 |
| 借阅 | `user1` 借阅《红楼梦》且已逾期（用于首页提醒演示） |

---

## 7. 接口需求概要

所有业务接口前缀为 `/api`，需登录接口在请求头携带：`Authorization: Bearer <token>`。

| 模块 | 基础路径 | 典型接口 |
|------|----------|----------|
| 用户 | `/api/user` | `POST /login`、`POST /logout`、`GET /list`、`GET /search` |
| 图书 | `/api/book` | `GET /list`、`GET /search`、`GET /available`、`POST /add` |
| 借阅 | `/api/borrow` | `GET /list`、`POST /borrow`、`POST /return`、`GET /overdue/reminder` |

---

## 8. 约束与假设

| 类型 | 说明 |
|------|------|
| 技术约束 | 后端 JDK 17 + Spring Boot 3.2 + MyBatis；前端 Vue 3 + Vite 5 + Element Plus |
| 运行假设 | 后端默认端口 **8080**，前端开发端口 **5173**，通过 Vite 代理 `/api` |
| 数据假设 | 单机演示与课程验收；不要求多实例共享数据库 |
| 范围外 | 暂不包含：邮件通知、罚款计费、多馆分库、移动端独立 App、生产级 MySQL 集群部署 |

---

## 9. 需求追溯

| 需求类别 | 设计文档章节 | 测试文档 |
|----------|--------------|----------|
| 功能需求 FR-* | `系统设计.md` 第 4～6 章 | `测试用例.md`、`测试计划.md` |
| 非功能 NFR-* | `系统设计.md` 第 7 章 | `性能与兼容性验收.md` |
| 用户操作 | — | `用户手册.md` |
| 进度与分工 | — | `任务分解表.md` |

---

## 10. 修订记录

| 版本 | 日期 | 修订说明 |
|------|------|----------|
| v1.0 | 2026-05-18 | 初稿，依据当前代码与 H2 内存库实际配置编写 |
